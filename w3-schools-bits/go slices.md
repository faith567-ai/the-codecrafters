
## Go Slices
Slices are like arrays, but more flexible they can grow or shrink in size also store many values of the same type.

- Create a Slice
Use []datatype{values}
From an array
Use make()
- Use a Slice
Get items using index (starts at 0)
[0] is first, [1] is second
- Add Items
Use append() to add elements