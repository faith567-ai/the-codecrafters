## Switch Statement

switch is used to choose one block of code to run, it checks a value and matches it with cases only the matching case runs (no need for break).

* How it works:
- Check the value once
- Compare with each case
- Run the matched case
- default runs if no match

* Multi-case Switch
A case can have more than one value.
